# midi_utils.py - MIDI utilities

def load_midi(file_path):
    """
    Load and parse a MIDI file.
    """
    print(f"Loading MIDI from {file_path}")

def save_midi(midi_data, output_path):
    """
    Save MIDI data to file.
    """
    print(f"Saving MIDI to {output_path}")
