# personalization.py - User input parsing and logic

def parse_user_input(user_profile):
    """
    Parse user input/preferences and return a structured config.
    """
    print(f"Parsing user profile: {user_profile}")
    return {"mood": "calm", "instrument": "piano"}
