# 🎵 Personalized AI Music Generator

Generate customized AI music based on mood, genre, and instruments using deep learning models.

## Usage
Run `app.py` after setting up dependencies in `requirements.txt`.

## Structure
Refer to the `src/` folder for generation and personalization logic.
